package dao;

import java.util.ArrayList;
import model.Item;

// DAO role: define and implement CRUD operations
public class ItemDAO {
    
        // CREATE an item in database
        public boolean addItem(Item item) {
            return false;
        }

        // READ one or more items from database
        public ArrayList<Item> getAllItems() {
            return null;
        }

        // UPDATE one or more items in database
        public boolean updateItem(Item item) {
            return false;
        }

        // DELETE one or more items in database
        public boolean deleteItem(Item item) {
            return false;
        }
}
